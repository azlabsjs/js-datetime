# Changelogs

## [v0.1.3] 2022-03-22

* Update build tools

## [0.1.3] 2022-03-24

* Add iso8691() static method to JSDate class to parse ISO8601 formatted date string
* Add isValid() method to check if a given date is valid
* Now isDate() check if the provided parameter is a Javascript date object
* Now Add support for `format` parameter in JsDate.create() method
    Supported format are:
    > YYYY<>MM<>DD or YY<>MM<>HH or YYYY<>DD<>MM or YY<>DD<>MM
    > MM<>DD<>YYYY or MM<>DD<>YYYY or MM<>YY<>DD or MM<>YYYY<>DDDD,
    > DD<>MM<>YYYY or DD<>MM<>YY or DD<>YYYY<>MM or DD<>YY<>MM

    Note: `<>` are placeholders for seperators. And the supported separators are `-` , ` ` , and `/`

    Note: For the Time part `HH:i:s` is the only format supported
